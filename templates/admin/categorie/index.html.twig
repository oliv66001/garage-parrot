{% extends 'base.html.twig' %}

{% block title %}
    Liste des catégories
{% endblock %}

{% block body %}
    <section class="container">
        <div class="row justify-content-center">
            {% include "_partials/_adminnav.html.twig" %}
            <main class="col p-2">
                <h1 class="text-center">Liste des catégories</h1>
                <table class="table">
                    <thead>
                        <tr class="text-center">
                            <th class="text-center">Nom</th>
                            <th class="text-center">Action</th>
                        </tr>
                    </thead>
                    {% for cat in categorie %}
                        <tr class="text-center">
                            <td>{{ cat.name }}</td>
                            {#<td> 
                                {% for vehicle in cat.vehicleType %}
                                    {{ vehicle.year|date('Y') }}
									
                                {% endfor %}</td>
								<td>  {% for vehicle in cat.vehicleType %}
                                  {{ vehicle.brand }}
									
                                {% endfor %}</td>#}
                           
                            <td class="align-middle">
                                <a href="{{ path('admin_categorie_edit', {'id': cat.id}) }}" class="btn btn2">Modifier</a>
                            </td>
                        </tr>
                    {% endfor %}

                </table>
            </main>
        </div>
    </section>
{% endblock %}
