<form method="DELETE" action="{{ path('admin_vehicle_delete_vehicle', {'id': vehicle.id}) }}">
   <a class="btn btn3" href="{{ path('admin_vehicle_delete_vehicle', {'id': vehicle.id}) }}" data-delete data-type="vehicle" data-token="{{ csrf_token('delete_vehicle' ~ vehicle.id) }}">Supprimer</a>
</form>